#include "MySearch.h"
#include <iostream>

using namespace mysearch;
using namespace std;

int main()
{
//	struct BinTree *head ;
//	struct BinTree *p = createTreenode(1);
//	printBinTree(head);
//	InsertBinTree(&head,p);
//	printBinTree(head);
//	p = createTreenode(2);
//	InsertBinTree(&head,p);
//	printBinTree(head);
//	struct BinTree *head =NULL;
//	struct BinTree *h2 =head;
//	struct BinTree **h = &head;
//	struct BinTree **h1 =&((*h)->lchild);
	return 0;
}

